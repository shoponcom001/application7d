<% if current_user.liked_by?(book.id) %>
  <%= link_to "いいねを外す", destroy_like_path(book), method: :delete, remote: true %><%= book.likes.count %>
<% else %>
  <%= link_to "いいね", create_like_path(book), method: :post, remote: true %><%= book.likes.count %>
<% end %>